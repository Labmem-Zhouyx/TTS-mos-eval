Place audio for this ABX pair here:

    reference.wav  (the X / reference audio)
    A.wav          (output of system_a as named in meta.json)
    B.wav          (output of system_b as named in meta.json)

The aggregator will report how often A beats B and the binomial p-value.
