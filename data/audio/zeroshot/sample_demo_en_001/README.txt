Place audio files for this sample here, named by their role / system:

    reference.wav       (reference clip for the target speaker)
    ground_truth.wav    (optional, the real recording)
    VoxCPM2.wav         (and one *.wav per system you want to compare)
    VoxCPM1.5.wav
    LongCat-Audio-DiT.wav
    FishAudio_S2.wav
    Qwen3-TTS.wav

The text is described in meta.json. Once you drop the files in, the
scanner picks them up automatically the next time the server is hit.
